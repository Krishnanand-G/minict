#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
BIN="${MINICT_BIN:-$ROOT/build/minict}"
if [[ ! -x "$BIN" ]]; then
  echo "minict binary not found: $BIN (run make all first)" >&2
  exit 1
fi
exec "$BIN" "$@"
